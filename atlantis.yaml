version: 3

# repos:
#   - id: /.*/
#     branch: /.*/
#     workflow: default
#     pre_workflow_hooks:
#       - run: |
#         echo "onnum illa" > /tmp/somefile.txt

projects:
- dir: .
  name: main_dir
- dir: no_cust
  name: no_cust
- dir: with_cust
  name: with_cust
  workflow: production

workflows:
   production:
    plan:
      steps:
      - run: |
          echo "generating backend.tf" >/tmp/test.txt
        # shell: bash
        # shellArgs: copy_backend.tf
      - init
      - plan:
          extra_args: ["-var-file", "production.tfvars"]